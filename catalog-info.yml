
# catalog-info.yml
# This file contains metadata for backstage
# Read more about available fields and annotations:
# https://github.com/tradeshift/backstage
#
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: puppet-logrotate
  description: |
    Puppet logrotate module
  annotations:
    github.com/project-slug: Tradeshift/puppet-logrotate # GitHub project name

spec:
  type: other
  owner: platform-ops
  lifecycle: experimental